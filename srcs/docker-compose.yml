name: inception

services:
  mariadb:
    build: ./requirements/mariadb
    image: mariadb:inception
    container_name: mariadb_container
    restart: always
    env_file:
      - .env
    networks:
      - bridge_network
    volumes:
      - mariadb-data:/var/lib/mysql

  wordpress:
    depends_on:
      - mariadb
    build: ./requirements/wordpress
    image: wordpress:inception
    container_name: wordpress_container
    restart: always
    env_file:
      - .env
    networks:
      - bridge_network
    volumes:
      - wp-data:/var/www/html

  nginx:
    depends_on:
      - wordpress
    build: ./requirements/nginx
    image: nginx:inception
    container_name: nginx_container
    restart: always
    env_file:
      - .env
    ports:
      - "443:443"
    networks:
      - bridge_network
    volumes:
      - wp-data:/var/www/html

  redis:
    depends_on:
      - nginx
    build: ./requirements/bonus/redis
    image: redis:inception
    container_name: redis_container
    restart: always
    env_file:
      - .env
    ports:
      - "6379:6379"
    networks:
      - bridge_network

  ftp:
    depends_on:
      - nginx
    build: ./requirements/bonus/FTP
    image: ftp:inception
    container_name: ftp_container
    restart: always
    env_file:
      - .env
    ports:
      - "21:21" # control
      - "20:20" # data active
      - "21100-21110:21100-21110" # data passive
    networks:
      - bridge_network
    volumes:
      - wp-data:/var/www/html

  static_website:
    build: ./requirements/bonus/static-website
    image: static_website:inception
    container_name: static_website_container
    restart: always
    ports:
      - "8080:80"
    networks:
      - bridge_network

  adminer:
    depends_on:
      - mariadb
    build: ./requirements/bonus/adminer
    image: adminer:inception
    container_name: adminer_container
    restart: always
    ports:
      - "8081:8080"
    networks:
      - bridge_network

  cadvisor:
    build: ./requirements/bonus/cadvisor
    image: cadvisor:inception
    container_name: cadvisor_container
    restart: always
    ports:
      - "8082:8080"
    networks:
      - bridge_network
    volumes:
      - /:/rootfs:ro            # disk usage of host
      - /var/run:/var/run:ro    # find and talk to Docker
      - /sys:/sys:ro            # CPU, RAM, network from kernel
      - /var/lib/docker/:/var/lib/docker:ro  # container names, metadata, image sizes

networks:
  bridge_network:
    driver: bridge

volumes:
  wp-data:
    driver: local # Store data on the local machine (not remote storage like NFS or cloud)
    driver_opts: # driver options
      type: none # Don’t use any special filesystem type (like ext4, nfs, etc.)
      o: bind # Bind (attach) this volume to an existing directory on the host
      device: /home/mdahani/data/wordpress-data # This is where data will physically be stored.
  mariadb-data:
    driver: local # Store data on the local machine (not remote storage like NFS or cloud)
    driver_opts: # driver options
      type: none # Don’t use any special filesystem type (like ext4, nfs, etc.)
      o: bind # Bind (attach) this volume to an existing directory on the host
      device: /home/mdahani/data/mariadb-data # This is where data will physically be stored.
